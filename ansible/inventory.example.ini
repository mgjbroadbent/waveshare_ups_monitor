; Copy to inventory.ini and edit. The daemon reports under the target's own hostname
; (slugified), so one group can hold every Pi you run this on.

[waveshare_ups]
raspberrypi.local

[waveshare_ups:vars]
ansible_user=pi
ansible_python_interpreter=/usr/bin/python3
